package com.gdx.game.system.defaults.settings

object SettingsProperties {
    object Graphics {
        const val RESOLUTION_X = "graphics.resolution.x"
        const val RESOLUTION_Y = "graphics.resolution.y"
        const val TEXTURE_SIZE = "graphics.textureSize"
        const val V_SYNC = "graphics.vSync"
    }
}