package com.gdx.game.system.defaults.settings

import com.badlogic.gdx.math.Vector2

object SettingsPropertiesDefaults {
    object Graphics {
        val RESOLUTION = Vector2(1280f, 720f)
        const val TEXTURE_SIZE = 1024
        const val V_SYNC = true
    }
}